[package]
name = "job-escrow"
version = "0.1.0"
description = "SolGrid Job Escrow — SPL token escrow for compute jobs"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "job_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { workspace = true }
anchor-spl = { workspace = true }
